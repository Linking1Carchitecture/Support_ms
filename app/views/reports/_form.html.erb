<%= form_with(model: report) do |form| %>
  <% if report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(report.errors.count, "error") %> prohibited this report from being saved:</h2>

      <ul>
        <% report.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :user_r %>
    <%= form.text_field :user_r %>
  </div>

  <div class="field">
    <%= form.label :img_r %>
    <%= form.text_field :img_r %>
  </div>

  <div class="field">
    <%= form.label :typer %>
    <%= form.number_field :typer %>
  </div>

  <div class="field">
    <%= form.label :offender %>
    <%= form.text_area :offender %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
